# Daily tracking (changes frequently, not needed in version control)
.STATUS

# macOS
.DS_Store

# Backup files (already have version control)
*.backup
backups/
node_modules
site/
.claude/settings.local.json
tests/logs/
WORKTREE-README.md

# Email dispatcher runtime cache
.flow/email-cache/
